int整型，整数
char存放一个字符
=是赋值
&&（并）左边表达式是假，则右边表达式不执行
||（或）左边是真，右边不执行
if （表达式）
     语句；
功能：表达式为真，语句执行
          表达式为假，语句不执行
float 定义小数  %f 匹配浮点型数据
“sum一般都是求和的意思。
 sum并不是C语言固有的关键字,一般是人们习惯性将变量或者求和函数定义为sum。
因为sum的英文意思为“求和。
个数一般用cnt表示
整数 / 整数 要得到整数，余数舍去，若分子分母中存在一个小数，则得到小数
判断一般在if中使用，不要和赋值混用
delta表示Δa，通常用于表示变量。
math.h头文件中声明了常用的一些数学运算，比如乘方，开方运算
double、float都是浮点型。double（双精度型）比float（单精度型）存的数据更准确些，占的空间也更大。double精度是float的两倍，所以需要更精确的计算常使用double。
在C语言中Val是一个将由数字符号（包括正负号、小数点）组成的字符型数据转换成相应的数值型数据的函数，语法格式是Val(string)，若字符串内出现非数字字符，那么只转换非数字字符前面的部分；若字符串的首字符不是数字符号，则返回数值零，但忽略前导空格。
break	跳出当前循环
continue	结束当前循环，开始下一轮循环
cpu内部存储数据的设备叫寄存器
